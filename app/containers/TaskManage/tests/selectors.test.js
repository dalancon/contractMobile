// import { fromJS } from 'immutable';
// import { makeSelectTaskManageDomain } from '../selectors';

// const selector = makeSelectTaskManageDomain();

describe('makeSelectTaskManageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
