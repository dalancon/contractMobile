// import { fromJS } from 'immutable';
// import { makeSelectViewContractDomain } from '../selectors';

// const selector = makeSelectViewContractDomain();

describe('makeSelectViewContractDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
