// import { fromJS } from 'immutable';
// import { makeSelectLayoutDomain } from '../selectors';

// const selector = makeSelectLayoutDomain();

describe('makeSelectLayoutDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
